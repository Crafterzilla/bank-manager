#include <stdio.h>
#include "include/input.h"
#include "include/menu.h"

int main() {
    // char* name = get_str();
    // printf("%s\n", name);
    // free(name);

    // login_prompt();
    user_creation_portal();
}